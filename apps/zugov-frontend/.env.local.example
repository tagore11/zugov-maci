VITE_API_URL=http://localhost:3001

# Required for wallet sign-in (embedded + external wallets) to work at all.
# Get an App ID: sign up at https://dashboard.privy.io, create an app, copy the App ID.
# This is a manual, one-time account-setup step — no automated tooling can do this part.
VITE_PRIVY_APP_ID=
